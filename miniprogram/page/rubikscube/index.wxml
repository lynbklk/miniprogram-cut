<xr-demo-viewer
  title="AR还原魔方"
  intro="AR还原魔方"
  code="{{xmlCode}}"
>
  <xr-ar-camera
    disable-scroll
    id="main-frame"
    width="{{renderWidth}}"
    height="{{renderHeight}}"
    style="width:{{width}}px;height:{{height}}px;top:{{top}}px;left:{{left}}px;display:block;"
  />
</xr-demo-viewer>
